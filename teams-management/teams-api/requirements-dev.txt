# Test-only dependencies (not installed into the runtime image).
#   python3.11 -m venv .venv && .venv/bin/pip install -r requirements.txt -r requirements-dev.txt
#   .venv/bin/python -m pytest tests/ -q
-r requirements.txt
pytest==8.3.4
# starlette 0.27 (via fastapi 0.104) predates httpx 0.28's Client() signature
# change, so TestClient breaks on newer httpx.
httpx==0.27.2
