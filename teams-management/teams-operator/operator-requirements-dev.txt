# Test-only dependencies (not installed into the runtime image).
#   python3 -m venv .venv && .venv/bin/pip install -r operator-requirements.txt -r operator-requirements-dev.txt
#   .venv/bin/python -m pytest tests/ -q
-r operator-requirements.txt
pytest==8.3.4
